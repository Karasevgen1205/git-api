import React, { Component } from "react";
import "./contributorsList.css";

export default class ContributorsList extends Component {
  constructor(props) {
    super(props);
  }

  // if (name != null) {

  // console.log(rep);
  //   });
  // }

  render() {
    // const gitApi = new GitApi();
    // const Contrib = gitApi
    //   .getAllContribForOneRepos("angular.js")
    //   .then((rep) => {
    //     rep.map((item) => <li>{item.login}</li>);
    //   });

    return {
      // Contrib,
    };
  }
}
